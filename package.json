{
  "name": "tomi",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@tomi/validation --filter=api",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@tomi/ts-config": "workspace:*",
    "turbo": "latest"
  }
}